# See bootstrap.example.toml for documentation of available options
#
change-id = "ignore"

[llvm]
download-ci-llvm = false

[target.x86_64-unknown-linux-gnu]
llvm-config = "../llvm-ropsched/build/bin/llvm-config"

[build]
extended = true

[rust]
channel = "dev"
